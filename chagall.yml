name: myapp

servers:
  - someserver
  - anotherserver

projects_folder: ~/chagall

dockerfile: Dockerfile
context: .

build_args:
  - --build-arg=key=value
  - --build-arg=key2=value2

cache_from: tmp/.buildx-cache
cache_to: tmp/.buildx-cache-new,mode=max

keep_releases: 3

compose_files:
  - compose.prod.yml

