#!/usr/bin/env ruby

require "chagall"

begin
  Chagall::Main.start(ARGV)
  if ARGV.any? { |arg| arg.include?("--debug") }
    require "debug"
    debugger
  end
rescue => e
  puts "  \e[31mERROR (#{e.class}): #{e.message}\e[0m"
  puts e.backtrace if ENV["VERBOSE"]
  exit 1
end
